using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class UI_Remind : MonoBehaviour {
    /*public GameObject UIRemind;
    private Text textContent;
    void Update()
    {
        if (UIManager.Instance.IsUILive("Remind"))
        {
            ShowText();
        }
    }
    void ShowText()
    {
        textContent = UIRemind.GetComponentsInChildren<GameObject>()[2].GetComponent<Text>();
        string _name = UIRemind.GetComponentsInChildren<Transform>()[2].name;
        string [] str  = _name.Split('(');
        textContent.text = "Here is " + str[0];
    }*/
}
